# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: smoke.spec.ts >> branded signed-out access >> keeps focus visible and reduces login motion when requested
- Location: e2e/smoke.spec.ts:47:3

# Error details

```
Error: expect(received).not.toBe(expected) // Object.is equality

Expected: not "none"
```

# Page snapshot

```yaml
- generic [ref=e1]:
  - main [ref=e3]:
    - generic [ref=e5]:
      - link "Ir al inicio de iJac" [ref=e6] [cursor=pointer]:
        - /url: /
        - img "iJac IT Solutions" [ref=e7]
      - paragraph [ref=e8]: Operaciones internas
      - heading "Ingresar a iJac" [level=1] [ref=e9]
      - paragraph [ref=e10]: Acceso seguro para gestionar clientes, órdenes de trabajo y calendario.
      - generic [ref=e11]:
        - generic [ref=e12]:
          - paragraph [ref=e13]: Acceso al panel
          - paragraph [ref=e14]: Usá tu cuenta autorizada de Google para continuar.
        - button "Continuar con Google" [active] [ref=e15]
        - generic [ref=e22]: Acceso protegido
      - paragraph [ref=e23]: Al continuar, confirmás que tenés autorización para acceder a este panel.
    - generic [ref=e24]: iJacIT Solutions · Buenos Aires
  - generic [ref=e27]:
    - button [ref=e28]
    - button [ref=e34]
    - button [ref=e38]
    - button [ref=e43]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('branded signed-out access', () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     await page.goto('/');
  6  |     await expect(page.getByTestId('sign-in-page')).toBeVisible();
  7  |   });
  8  | 
  9  |   test('shows iJac identity and one supported sign-in action', async ({ page }) => {
  10 |     const signInPage = page.getByTestId('sign-in-page');
  11 |     await expect(page).toHaveTitle(/iJac Operations/);
  12 |     await expect(signInPage.getByRole('heading', { name: 'Ingresar a iJac' })).toBeVisible();
  13 |     await expect(signInPage.getByText('Acceso al panel')).toBeVisible();
  14 |     await expect(signInPage.getByRole('button', { name: 'Continuar con Google' })).toBeVisible();
  15 |     await expect(signInPage.getByRole('button')).toHaveCount(1);
  16 |     await expect(signInPage.locator('input')).toHaveCount(0);
  17 |   });
  18 | 
  19 |   test('fits the centered desktop composition without clipping', async ({ page }) => {
  20 |     await page.setViewportSize({ width: 1440, height: 1000 });
  21 | 
  22 |     const panel = page.locator('.login-panel');
  23 |     const bounds = await panel.boundingBox();
  24 |     const viewport = page.viewportSize();
  25 | 
  26 |     expect(bounds).not.toBeNull();
  27 |     expect(bounds!.width).toBeLessThanOrEqual(384);
  28 |     expect(bounds!.x).toBeGreaterThan(0);
  29 |     expect(bounds!.x + bounds!.width).toBeLessThan(viewport!.width);
  30 |     expect(await page.evaluate(() => document.documentElement.scrollWidth)).toBeLessThanOrEqual(viewport!.width);
  31 |   });
  32 | 
  33 |   test('adapts on mobile with a usable primary target and no horizontal overflow', async ({ page }) => {
  34 |     await page.setViewportSize({ width: 375, height: 812 });
  35 | 
  36 |     const button = page.getByRole('button', { name: 'Continuar con Google' });
  37 |     const bounds = await button.boundingBox();
  38 |     const viewport = page.viewportSize();
  39 | 
  40 |     expect(bounds).not.toBeNull();
  41 |     expect(bounds!.height).toBeGreaterThanOrEqual(44);
  42 |     expect(bounds!.x).toBeGreaterThanOrEqual(0);
  43 |     expect(bounds!.x + bounds!.width).toBeLessThanOrEqual(viewport!.width);
  44 |     expect(await page.evaluate(() => document.documentElement.scrollWidth)).toBeLessThanOrEqual(viewport!.width);
  45 |   });
  46 | 
  47 |   test('keeps focus visible and reduces login motion when requested', async ({ page }) => {
  48 |     const button = page.getByRole('button', { name: 'Continuar con Google' });
  49 |     await button.focus();
  50 |     await expect(button).toBeFocused();
  51 | 
  52 |     const focusShadow = await button.evaluate((element) => getComputedStyle(element).boxShadow);
> 53 |     expect(focusShadow).not.toBe('none');
     |                             ^ Error: expect(received).not.toBe(expected) // Object.is equality
  54 | 
  55 |     await page.emulateMedia({ reducedMotion: 'reduce' });
  56 |     const animationDuration = await page.locator('.login-reveal').evaluate((element) => getComputedStyle(element).animationDuration);
  57 |     expect(animationDuration).toBe('0.01ms');
  58 |   });
  59 | });
  60 | 
```