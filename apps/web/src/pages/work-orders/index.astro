---
import Layout from '../../layouts/Layout.astro';
import { Protected } from '../../components/layout/Protected';
import { AppShell } from '../../components/layout/AppShell';
import { WorkOrderManager } from '../../components/work-orders/WorkOrderManager';
---

<Layout titleKey="title.workOrders">
  <Protected client:load>
    <AppShell client:load>
      <h1 class="text-2xl font-semibold text-fg-primary" data-i18n="workOrders.pageHeading">Órdenes de trabajo</h1>
      <p class="mt-2 text-fg-secondary" data-i18n="workOrders.pageDescription">Gestioná las órdenes de trabajo.</p>
      <div class="mt-6">
        <WorkOrderManager client:load />
      </div>
    </AppShell>
  </Protected>
</Layout>
